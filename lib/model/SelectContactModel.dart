class SelectContactModel {
  String name;
  String status;
  bool select = false;
  SelectContactModel(
      {
        required this.name,
        required this.status,
        required this.select
      }
      );
}